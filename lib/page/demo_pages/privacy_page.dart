import 'package:flutter/material.dart';

class DemoPrivacyPage extends StatelessWidget {
  const DemoPrivacyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("PRIVACY PAGE"),
      ),
    );
  }
}
