import 'package:flutter/material.dart';

class MobileDateTimePicker extends StatelessWidget {
  const MobileDateTimePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}