import 'package:flutter/material.dart';

class MaterialDatePicker extends StatelessWidget {
  const MaterialDatePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}